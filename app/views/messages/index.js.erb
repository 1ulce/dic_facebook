$(".conversation-area").html("<%= escape_javascript(render partial: 'messages/index', locals: { users: @users, conversation: @conversation, messages: @messages, message: @message } ) %>")
$(".conversation-area").show();
$('.conversation-messages').animate({scrollTop: $('.conversation-messages')[0].scrollHeight}, 'slow');