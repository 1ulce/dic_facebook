$(".conversation-messages").html("<%= escape_javascript(render partial: 'messages/messages', locals: { messages: @messages } ) %>")
$('#message_content').val('')
$('.conversation-messages').animate({scrollTop: $('.conversation-messages')[0].scrollHeight}, 'fast');