<div class="wrapper">
  <div class= "row">
    <div class= "col-xs-3 col-gray user-info">
      <%= @user.picture %>
      <%= @user.name %>
      <button>編集</button>
    </div>
    <div class= "col-xs-3">
      <div class= "index-form row col-white">
        <%= form_for(@topic, url: topics_path, remote: true) do |f| %>
          <% if @topic.errors.any? %>
            <% @topic.errors.full_messages.each do |msg| %>
              <div class= "alert alert-warning" role="alert">
              <p><%= msg %></p>
            </div>
            <% end %>
          <% end %>
          <%= f.text_area :content,type: :text,  :placeholder => '今何してる？', class: 'form-control' %>
          <br>
          <%= f.submit '投稿', class: 'btn btn-primary btn-block' %>
        <% end %>
      </div>
      <%= render partial: 'topics/show', locals: { topics: @topics } %>
    </div>
    <div class= "col-xs-3">
    </div>
    <div class= "col-xs-3 col-gray">
    </div>
  </div>
</div>