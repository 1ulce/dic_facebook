var topic_id = <%= @comment.topic.id %>
$(`.topic_area_${topic_id} .comments_area`).html("<%= j(render 'comments/index', { comments: @comment.topic.comments, topic: @comment.topic }) %>")
$(':text').val('')